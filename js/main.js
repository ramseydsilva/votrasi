require.config({
    paths: {
        "jquery": "jquery-2.1.0.min",
        "underscore": "underscore-min",
        "bootstrap": "bootstrap.min",
        "text": "plugins/text"
    },
    shim: {
        "bootstrap": ["jquery"]
    }
});

define([
], function(){
});
